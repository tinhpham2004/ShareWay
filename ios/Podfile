platform :ios, '12.0'

# Uncomment the next line if you're using Swift
# use_frameworks!

target 'Runner' do
  # Flutter's generated Podfile contents
  use_frameworks!
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end
