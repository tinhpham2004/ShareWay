import 'package:flutter/material.dart';

class HitchRideScreen extends StatelessWidget {
  const HitchRideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Hitch Ride Screen',
        ),
      ),
    );
  }
}
